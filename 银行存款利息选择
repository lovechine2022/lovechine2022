#include <stdio.h>
#include <math.h>//使用pow(x,y)指数形式需使用
int main()
{
	double p=1;
	int n=0;
	scanf("%d",&n);
	switch(n)
	{
	case 1://一次存5年
		p=1000*(1+5*0.03);
		printf("5年期本息和为%.3f",p);
		break;
	case 2://先存2年，到期后将本息再存3年期
		p=1000*(1+2*0.021)+1000*(1+3*0.0275);
		printf("本息和为%.3f",p);
		break;
	case 3://存1年期，到期后再将本息存1年期，连续5次
			p=p*1000*pow(1+0.015,5);//1000*(1+0.015)^5形式不能使用故用pow(1+0.015,5)代替
		printf("本息和为%.3f",p);
		break;
	case 4://活期存款,5年的利息
		p=1000*pow(1+0.0035/4,4*5); 
		printf("本息和为%.3f",p);
		break;
	default:
		printf("输入错误");
	}
	return 0;
}


注意指数形式不能使用小数和使用的目标不能含有小数
所以故使用pow（x，y）代替
使用pow(x,y)需加#include <math.h>
